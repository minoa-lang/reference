# Unit expressions
```
<unit-expr> := '(' ')'
```

A unit expressions is an empty expression that does nothing and returns a value of the unit type.

> _Example_
> ```
> // Assign a unit value to `a`
> a := ();
> ```